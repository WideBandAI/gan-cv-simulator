#[derive(Debug)]
pub struct DeviceStructure {
    pub depth: Vec<f64>,
    pub me: Vec<f64>,
    pub er_s: Vec<f64>,
    pub eg: Vec<f64>,
    pub dec: Vec<f64>,
    pub nd: Vec<f64>,
    pub end: Vec<f64>,
}
